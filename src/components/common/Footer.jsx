import styled from "styled-components";

export const Footer = () => {
  return <StyledFooterContainer>aa</StyledFooterContainer>;
};

const StyledFooterContainer = styled.footer`
  background-color: blue;
`;
